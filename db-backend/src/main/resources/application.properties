spring.application.name=db-backend

# MySQL connection settings
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/shared_space_booking_db?createDatabaseIfNotExist=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:wys13389978139}

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:true}
spring.jpa.open-in-view=false

app.auth.token-ttl-hours=${AUTH_TOKEN_TTL_HOURS:24}
app.bootstrap.admin-user-no=${APP_BOOTSTRAP_ADMIN_USER_NO:admin}
app.bootstrap.admin-password=${APP_BOOTSTRAP_ADMIN_PASSWORD:admin123456}
app.bootstrap.admin-real-name=${APP_BOOTSTRAP_ADMIN_REAL_NAME:System Admin}


server.port=8080
